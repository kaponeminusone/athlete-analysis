# Web cloud (HopLab)

Aquí irá el frontend adaptado desde `ui-family/` para desplegar en **Vercel** o **Render**.

Diferencias clave vs lab local:

- Sin proxy Vite a `127.0.0.1:8000`
- `VITE_API_BASE` + pantalla **Conectar motor** (URL del túnel Colab)
- Build estático únicamente

Ver [`../docs/COLAB_PORT_PLAN.md`](../docs/COLAB_PORT_PLAN.md) §7.
